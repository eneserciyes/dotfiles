#!/usr/bin/zsh
if [[ -z $STOW_FOLDERS ]]; then
   STOW_FOLDERS="nvim-kickstart,zsh,tmux,personal,bin,alacritty,oh-my-zsh"
fi

if [[ -z $DOTFILES ]]; then
    DOTFILES=$HOME/ws/dotfiles
fi

STOW_FOLDERS=$STOW_FOLDERS DOTFILES=$DOTFILES $DOTFILES/install
